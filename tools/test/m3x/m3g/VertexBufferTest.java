package m3x.m3g;


public class VertexBufferTest extends AbstractTestCase
{
  public void testSerializationAndDeseriliazation1()
  {   
    /*ObjectIndex[] animationTracks = getAnimationTracks();
    UserParameter[] userParameters = getUserParameters();
    TextureCoordinate textureCoord = new TextureCoordinate(new ObjectIndex(4),
                                                           new float[] {1.0f, 1.1f, 1.2f},
                                                           0.5f);
    TextureCoordinate[] textureCoords = {textureCoord};
    VertexBuffer buffer = new VertexBuffer(animationTracks,
                                           userParameters,
                                           new ColorRGBA(0.1f, 0.2f, 0.3f, 1.0f),
                                           new ObjectIndex(1),
                                           new float[] {0.4f, 0.5f, 0.6f},
                                           0.7f,
                                           new ObjectIndex(2),
                                           new ObjectIndex(3),
                                           textureCoords);
                                           
                                           
    try
    {   
      byte[] serialized = M3GSupport.objectToBytes(buffer);
      VertexBuffer deserialized = (VertexBuffer)M3GSupport.bytesToObject(serialized, VertexBuffer.class);
      this.doTestAccessors(buffer, deserialized);
    }
    catch (Exception e)
    {
      e.printStackTrace();
      fail(e.getMessage());
    }*/
  }
}
